import { CountItemPipe } from './count-item.pipe';

describe('CountItemPipe', () => {
  it('create an instance', () => {
    const pipe = new CountItemPipe();
    expect(pipe).toBeTruthy();
  });
});
